# tts
It allows users to convert written text into natural-sounding speech directly in the browser without requiring any external libraries or APIs.  Users can enter or paste text, select from available system voices, and customize the speech output by adjusting **speed (rate)** and **pitch** controls. 
